<h1 class="title is-1">
  <%= @page.title %>
</h1>

<div class="fr-view">
  <%= @page.content.html_safe %>
</div>

<% if admin? %>
  <%= link_to 'Szerkesztés', edit_page_path(@page), class: 'button is-secondary' %>
<% end%>
