<h1>Kategóriák</h1>

<table>
  <thead>
    <tr>
      <th>Név</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <td><%= link_to 'Szerkesztés', edit_category_path(category) %></td>
        <td><%= link_to 'Törlés', category, method: :delete, data: { confirm: 'A kategória törlésével a hozzá tartozó szendvicseket is törlöd! Biztosan törlöd?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Új kategória', new_category_path %>
