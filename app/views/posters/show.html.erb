<h2>
  <%= @poster.name %>
</h2>

<p>
  <%= image_tag @poster.image.url, width: '80%' if @poster.image? %>
</p>

<% if admin? %>
  <%= link_to 'Szerkesztés', edit_poster_path(@poster) %> |
<% end %>
<%= link_to 'Vissza', posters_path %>
